//const mongoose = require('mongoose');// responsavel pela conexao mapeamento dos objetos armazenados no mongodb
//module.exports = mongoose.connect('mongodb://localhost/db_loteria'); // exporta um objeto que mantem conexao com o mongodb
